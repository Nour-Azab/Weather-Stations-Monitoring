FROM python:3.12-slim

WORKDIR /app

# duckdb  → reads .parquet files
# elasticsearch → official ES Python client
# requests → used for health-check polling
RUN pip install --no-cache-dir duckdb==1.1.3 elasticsearch==8.13.2 requests

COPY indexer.py .

CMD ["python", "-u", "indexer.py"]